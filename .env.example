# Environment Variables for Portfolio Site
# Copy this file to .env and fill in your values

# GitHub Personal Access Tokens
# Create at: https://github.com/settings/tokens

# GH_GIST_TOKEN: Used to update your resume gist via npm run update-registry
# Required scopes: gist (read/write access to gists)
GH_GIST_TOKEN=your_github_personal_access_token_here

# GH_DEV_TOKEN: Used during development to fetch GitHub projects and avoid rate limiting
# Required scopes: public_repo (or none for public repos only)
GH_DEV_TOKEN=your_github_personal_access_token_here
